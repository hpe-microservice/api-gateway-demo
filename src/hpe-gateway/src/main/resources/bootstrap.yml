server:
  port: 8080

spring:
  application:
    name: hpe-gateway

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
  ignoredServices: '/open/*'
